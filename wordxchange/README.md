# WordXchange

A dependency-free recreation of the original WordXchange prototype for the portfolio.

Open `index.html` through a local web server. The game ships with a compact offline word library and expands it from the public `dwyl/english-words` dictionary when a network connection is available.

The original Angular source is preserved in `../wordxchange-source/`.
